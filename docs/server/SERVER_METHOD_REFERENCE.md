# MeetSpace Server — Полный метод-уровневый reference
Автоматически сгенерированный reference по исходникам репозитория.
Документ предназначен для быстрого технического ориентирования, code review и онбординга.

## Покрытие
- **Исходных файлов обработано:** 8
- **Сигнатур методов/функций:** 188

## Формат описания
Для каждой сигнатуры указаны назначение, параметры и возвращаемое значение.

### `apps/mediasoup-server/scripts/stress-media-plane.js`
- **Пространство/модуль:** `(js-module)`
- **Абстракции (типы):** BackendClient
- **Количество методов/функций:** 14

#### `function readIntEnv(name, fallbackValue, minValue, maxValue)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `name` — входной параметр метода.
  - `fallbackValue` — входной параметр метода.
  - `minValue` — входной параметр метода.
  - `maxValue` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function readFloatEnv(name, fallbackValue, minValue, maxValue)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `name` — входной параметр метода.
  - `fallbackValue` — входной параметр метода.
  - `minValue` — входной параметр метода.
  - `maxValue` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function quantile(values, percentile)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `values` — входной параметр метода.
  - `percentile` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function formatMs(value)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `value` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function runWithConcurrency(items, concurrency, worker)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `items` — входной параметр метода.
  - `concurrency` — входной параметр метода.
  - `worker` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `function findCodec(routerCaps, kind)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `routerCaps` — входной параметр метода.
  - `kind` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function buildRtpParameters(routerCaps, kind, ssrcSeed)`
- **Контекст:** `(global)`
- **Назначение:** Собирает производную структуру/результат из входных данных.
- **Параметры:**
  - `routerCaps` — входной параметр метода.
  - `kind` — входной параметр метода.
  - `ssrcSeed` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function buildRtpCapabilities(routerCaps)`
- **Контекст:** `(global)`
- **Назначение:** Собирает производную структуру/результат из входных данных.
- **Параметры:**
  - `routerCaps` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function main()`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async connect()`
- **Контекст:** `BackendClient`
- **Назначение:** Устанавливает соединение с внешним или внутренним сервисом.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `handleMessage(rawBuffer)`
- **Контекст:** `BackendClient`
- **Назначение:** Обрабатывает событие/команду и запускает соответствующий сценарий.
- **Параметры:**
  - `rawBuffer` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `rejectAllPending(error)`
- **Контекст:** `BackendClient`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `error` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async request(operation, payload)`
- **Контекст:** `BackendClient`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `operation` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `close()`
- **Контекст:** `BackendClient`
- **Назначение:** Закрывает ресурс и выполняет очистку зависимых сущностей.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

---

### `apps/mediasoup-server/scripts/stress-ws-signaling.js`
- **Пространство/модуль:** `(js-module)`
- **Абстракции (типы):** SignalingClient
- **Количество методов/функций:** 12

#### `function readIntEnv(name, fallbackValue, minValue, maxValue)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `name` — входной параметр метода.
  - `fallbackValue` — входной параметр метода.
  - `minValue` — входной параметр метода.
  - `maxValue` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function readFloatEnv(name, fallbackValue, minValue, maxValue)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `name` — входной параметр метода.
  - `fallbackValue` — входной параметр метода.
  - `minValue` — входной параметр метода.
  - `maxValue` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function quantile(values, percentile)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `values` — входной параметр метода.
  - `percentile` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function runWithConcurrency(items, concurrency, worker)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `items` — входной параметр метода.
  - `concurrency` — входной параметр метода.
  - `worker` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `function formatMs(value)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `value` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function shuffleInPlace(values)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `values` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function main()`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async connect()`
- **Контекст:** `SignalingClient`
- **Назначение:** Устанавливает соединение с внешним или внутренним сервисом.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `handleMessage(rawBuffer)`
- **Контекст:** `SignalingClient`
- **Назначение:** Обрабатывает событие/команду и запускает соответствующий сценарий.
- **Параметры:**
  - `rawBuffer` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `rejectAllPending(error)`
- **Контекст:** `SignalingClient`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `error` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async request(operation, payload)`
- **Контекст:** `SignalingClient`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `operation` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `close()`
- **Контекст:** `SignalingClient`
- **Назначение:** Закрывает ресурс и выполняет очистку зависимых сущностей.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

---

### `apps/mediasoup-server/src/server.js`
- **Пространство/модуль:** `(js-module)`
- **Абстракции (типы):** (не обнаружены)
- **Количество методов/функций:** 73

#### `function defaultWorkerCount()`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function clampWorkerCount(value)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `value` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function readBoundedIntFromEnv(name, fallbackValue, minValue, maxValue)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `name` — входной параметр метода.
  - `fallbackValue` — входной параметр метода.
  - `minValue` — входной параметр метода.
  - `maxValue` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function parseBooleanEnv(name, fallbackValue = false)`
- **Контекст:** `(global)`
- **Назначение:** Разбирает входные данные и преобразует в структурированное представление.
- **Параметры:**
  - `name` — входной параметр метода.
  - `fallbackValue = false` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function parseIceServers(raw)`
- **Контекст:** `(global)`
- **Назначение:** Разбирает входные данные и преобразует в структурированное представление.
- **Параметры:**
  - `raw` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function readTlsFile(pathValue, fieldName)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `pathValue` — входной параметр метода.
  - `fieldName` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function buildTlsServerOptions()`
- **Контекст:** `(global)`
- **Назначение:** Собирает производную структуру/результат из входных данных.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function safeSend(ws, payload)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `ws` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function withOperationTimeout(promise, timeoutMs)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `promise` — входной параметр метода.
  - `timeoutMs` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function clearSocketQueue(runtimeState)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `runtimeState` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function handleWsMessage(ws, rawBuffer)`
- **Контекст:** `(global)`
- **Назначение:** Обрабатывает событие/команду и запускает соответствующий сценарий.
- **Параметры:**
  - `ws` — входной параметр метода.
  - `rawBuffer` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function drainSocketQueue(ws, runtimeState)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `ws` — входной параметр метода.
  - `runtimeState` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `function startHeartbeatLoop()`
- **Контекст:** `(global)`
- **Назначение:** Запускает фоновый цикл, процесс или жизненный цикл компонента.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function totalPeersCount()`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function countEntitiesByRoom(entries, roomId)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `entries` — входной параметр метода.
  - `roomId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function buildWorkerLoadSnapshot()`
- **Контекст:** `(global)`
- **Назначение:** Собирает производную структуру/результат из входных данных.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function ensureWorkerPeerCapacityForJoin(roomId, peerId)`
- **Контекст:** `(global)`
- **Назначение:** Гарантирует достижение требуемого состояния или ресурса.
- **Параметры:**
  - `roomId` — входной параметр метода.
  - `peerId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function ensureEntityCapacity(currentValue, maxValue, entityName)`
- **Контекст:** `(global)`
- **Назначение:** Гарантирует достижение требуемого состояния или ресурса.
- **Параметры:**
  - `currentValue` — входной параметр метода.
  - `maxValue` — входной параметр метода.
  - `entityName` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function ensureRoomCapacityForJoin(roomId, peerId)`
- **Контекст:** `(global)`
- **Назначение:** Гарантирует достижение требуемого состояния или ресурса.
- **Параметры:**
  - `roomId` — входной параметр метода.
  - `peerId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function ensurePeerJoined(roomId, peerId)`
- **Контекст:** `(global)`
- **Назначение:** Гарантирует достижение требуемого состояния или ресурса.
- **Параметры:**
  - `roomId` — входной параметр метода.
  - `peerId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function enforcePeerRateLimit(peerId)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `peerId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function resolvePeerIdForRateLimit(payload)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function failWithCode(code, message)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `code` — входной параметр метода.
  - `message` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function ensureNotShuttingDown()`
- **Контекст:** `(global)`
- **Назначение:** Гарантирует достижение требуемого состояния или ресурса.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function handleRequest(request)`
- **Контекст:** `(global)`
- **Назначение:** Обрабатывает событие/команду и запускает соответствующий сценарий.
- **Параметры:**
  - `request` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opGetCapabilities(id)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `function toFiniteNumber(value)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `value` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function summarizeStatsRows(rows)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `rows` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function normalizeInjectedStats(value)`
- **Контекст:** `(global)`
- **Назначение:** Нормализует формат входных данных для дальнейшей обработки.
- **Параметры:**
  - `value` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function buildInjectedStatsFromPayload(payload)`
- **Контекст:** `(global)`
- **Назначение:** Собирает производную структуру/результат из входных данных.
- **Параметры:**
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function createEmptyStatsSummary()`
- **Контекст:** `(global)`
- **Назначение:** Создаёт новый объект/ресурс и подготавливает его к использованию.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function opGetMediaStats(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opCreateRouter(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opJoinPeer(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opLeavePeer(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opClosePeer(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opCreateWebRtcTransport(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opConnectDtls(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opAddIceCandidate(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opProduce(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opPauseProducer(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opResumeProducer(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opCloseProducer(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opConsume(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opResumeConsumer(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `function makeSuccess(id, message, data, router)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `id` — входной параметр метода.
  - `message` — входной параметр метода.
  - `data` — входной параметр метода.
  - `router` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function makeFailure(id, code, message)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `id` — входной параметр метода.
  - `code` — входной параметр метода.
  - `message` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function requireString(value, fieldName)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `value` — входной параметр метода.
  - `fieldName` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function ensurePeerSet(roomId)`
- **Контекст:** `(global)`
- **Назначение:** Гарантирует достижение требуемого состояния или ресурса.
- **Параметры:**
  - `roomId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function getTransportEntry(transportId)`
- **Контекст:** `(global)`
- **Назначение:** Получает и возвращает данные из текущего контекста/хранилища.
- **Параметры:**
  - `transportId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function createWorkerContext()`
- **Контекст:** `(global)`
- **Назначение:** Создаёт новый объект/ресурс и подготавливает его к использованию.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `function removeWorkerContext(workerContext)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `workerContext` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function getWorkerContextById(workerId)`
- **Контекст:** `(global)`
- **Назначение:** Получает и возвращает данные из текущего контекста/хранилища.
- **Параметры:**
  - `workerId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function workerDiagnostics()`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function pickWorkerContextForRoom(roomId)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `roomId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function getRoomWorkerContext(roomId)`
- **Контекст:** `(global)`
- **Назначение:** Получает и возвращает данные из текущего контекста/хранилища.
- **Параметры:**
  - `roomId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function getRoomRouter(roomId)`
- **Контекст:** `(global)`
- **Назначение:** Получает и возвращает данные из текущего контекста/хранилища.
- **Параметры:**
  - `roomId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function ensureWorkerPool()`
- **Контекст:** `(global)`
- **Назначение:** Гарантирует достижение требуемого состояния или ресурса.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function ensureCapabilityRouter()`
- **Контекст:** `(global)`
- **Назначение:** Гарантирует достижение требуемого состояния или ресурса.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function ensureRoomRouter(roomId)`
- **Контекст:** `(global)`
- **Назначение:** Гарантирует достижение требуемого состояния или ресурса.
- **Параметры:**
  - `roomId` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `function safeCloseTransportById(transportId)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `transportId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function safeCloseProducerById(producerId)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `producerId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function safeCloseConsumerById(consumerId)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `consumerId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function closePeerResources(roomId, peerId)`
- **Контекст:** `(global)`
- **Назначение:** Закрывает ресурс и выполняет очистку зависимых сущностей.
- **Параметры:**
  - `roomId` — входной параметр метода.
  - `peerId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function closeRoom(roomId)`
- **Контекст:** `(global)`
- **Назначение:** Закрывает ресурс и выполняет очистку зависимых сущностей.
- **Параметры:**
  - `roomId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function normalizeWsPath(pathValue)`
- **Контекст:** `(global)`
- **Назначение:** Нормализует формат входных данных для дальнейшей обработки.
- **Параметры:**
  - `pathValue` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function log(message, level = 'info', extra = null)`
- **Контекст:** `(global)`
- **Назначение:** Формирует и отправляет диагностическую запись.
- **Параметры:**
  - `message` — входной параметр метода.
  - `level = 'info'` — входной параметр метода.
  - `extra = null` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function audit(peerId, operation, detail)`
- **Контекст:** `(global)`
- **Назначение:** Записывает аудит-событие для последующего анализа.
- **Параметры:**
  - `peerId` — входной параметр метода.
  - `operation` — входной параметр метода.
  - `detail` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function verifyAuthToken(tokenString)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `tokenString` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function buildHealthResponse()`
- **Контекст:** `(global)`
- **Назначение:** Собирает производную структуру/результат из входных данных.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function buildPrometheusMetrics()`
- **Контекст:** `(global)`
- **Назначение:** Собирает производную структуру/результат из входных данных.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function startQosCollectionLoop()`
- **Контекст:** `(global)`
- **Назначение:** Запускает фоновый цикл, процесс или жизненный цикл компонента.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function shutdown()`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

---

### `deploy/meetspace-server-31.177.83.146/apps/mediasoup-server/scripts/stress-media-plane.js`
- **Пространство/модуль:** `(js-module)`
- **Абстракции (типы):** BackendClient
- **Количество методов/функций:** 14

#### `function readIntEnv(name, fallbackValue, minValue, maxValue)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `name` — входной параметр метода.
  - `fallbackValue` — входной параметр метода.
  - `minValue` — входной параметр метода.
  - `maxValue` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function readFloatEnv(name, fallbackValue, minValue, maxValue)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `name` — входной параметр метода.
  - `fallbackValue` — входной параметр метода.
  - `minValue` — входной параметр метода.
  - `maxValue` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function quantile(values, percentile)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `values` — входной параметр метода.
  - `percentile` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function formatMs(value)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `value` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function runWithConcurrency(items, concurrency, worker)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `items` — входной параметр метода.
  - `concurrency` — входной параметр метода.
  - `worker` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `function findCodec(routerCaps, kind)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `routerCaps` — входной параметр метода.
  - `kind` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function buildRtpParameters(routerCaps, kind, ssrcSeed)`
- **Контекст:** `(global)`
- **Назначение:** Собирает производную структуру/результат из входных данных.
- **Параметры:**
  - `routerCaps` — входной параметр метода.
  - `kind` — входной параметр метода.
  - `ssrcSeed` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function buildRtpCapabilities(routerCaps)`
- **Контекст:** `(global)`
- **Назначение:** Собирает производную структуру/результат из входных данных.
- **Параметры:**
  - `routerCaps` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function main()`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async connect()`
- **Контекст:** `BackendClient`
- **Назначение:** Устанавливает соединение с внешним или внутренним сервисом.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `handleMessage(rawBuffer)`
- **Контекст:** `BackendClient`
- **Назначение:** Обрабатывает событие/команду и запускает соответствующий сценарий.
- **Параметры:**
  - `rawBuffer` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `rejectAllPending(error)`
- **Контекст:** `BackendClient`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `error` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async request(operation, payload)`
- **Контекст:** `BackendClient`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `operation` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `close()`
- **Контекст:** `BackendClient`
- **Назначение:** Закрывает ресурс и выполняет очистку зависимых сущностей.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

---

### `deploy/meetspace-server-31.177.83.146/apps/mediasoup-server/scripts/stress-ws-signaling.js`
- **Пространство/модуль:** `(js-module)`
- **Абстракции (типы):** SignalingClient
- **Количество методов/функций:** 12

#### `function readIntEnv(name, fallbackValue, minValue, maxValue)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `name` — входной параметр метода.
  - `fallbackValue` — входной параметр метода.
  - `minValue` — входной параметр метода.
  - `maxValue` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function readFloatEnv(name, fallbackValue, minValue, maxValue)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `name` — входной параметр метода.
  - `fallbackValue` — входной параметр метода.
  - `minValue` — входной параметр метода.
  - `maxValue` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function quantile(values, percentile)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `values` — входной параметр метода.
  - `percentile` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function runWithConcurrency(items, concurrency, worker)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `items` — входной параметр метода.
  - `concurrency` — входной параметр метода.
  - `worker` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `function formatMs(value)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `value` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function shuffleInPlace(values)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `values` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function main()`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async connect()`
- **Контекст:** `SignalingClient`
- **Назначение:** Устанавливает соединение с внешним или внутренним сервисом.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `handleMessage(rawBuffer)`
- **Контекст:** `SignalingClient`
- **Назначение:** Обрабатывает событие/команду и запускает соответствующий сценарий.
- **Параметры:**
  - `rawBuffer` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `rejectAllPending(error)`
- **Контекст:** `SignalingClient`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `error` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async request(operation, payload)`
- **Контекст:** `SignalingClient`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `operation` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `close()`
- **Контекст:** `SignalingClient`
- **Назначение:** Закрывает ресурс и выполняет очистку зависимых сущностей.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

---

### `deploy/meetspace-server-31.177.83.146/apps/mediasoup-server/src/server.js`
- **Пространство/модуль:** `(js-module)`
- **Абстракции (типы):** (не обнаружены)
- **Количество методов/функций:** 61

#### `function defaultWorkerCount()`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function clampWorkerCount(value)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `value` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function readBoundedIntFromEnv(name, fallbackValue, minValue, maxValue)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `name` — входной параметр метода.
  - `fallbackValue` — входной параметр метода.
  - `minValue` — входной параметр метода.
  - `maxValue` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function parseBooleanEnv(name, fallbackValue = false)`
- **Контекст:** `(global)`
- **Назначение:** Разбирает входные данные и преобразует в структурированное представление.
- **Параметры:**
  - `name` — входной параметр метода.
  - `fallbackValue = false` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function readTlsFile(pathValue, fieldName)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `pathValue` — входной параметр метода.
  - `fieldName` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function buildTlsServerOptions()`
- **Контекст:** `(global)`
- **Назначение:** Собирает производную структуру/результат из входных данных.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function safeSend(ws, payload)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `ws` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function withOperationTimeout(promise, timeoutMs)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `promise` — входной параметр метода.
  - `timeoutMs` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function handleWsMessage(ws, rawBuffer)`
- **Контекст:** `(global)`
- **Назначение:** Обрабатывает событие/команду и запускает соответствующий сценарий.
- **Параметры:**
  - `ws` — входной параметр метода.
  - `rawBuffer` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function drainSocketQueue(ws, runtimeState)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `ws` — входной параметр метода.
  - `runtimeState` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `function startHeartbeatLoop()`
- **Контекст:** `(global)`
- **Назначение:** Запускает фоновый цикл, процесс или жизненный цикл компонента.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function totalPeersCount()`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function countEntitiesByRoom(entries, roomId)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `entries` — входной параметр метода.
  - `roomId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function ensureEntityCapacity(currentValue, maxValue, entityName)`
- **Контекст:** `(global)`
- **Назначение:** Гарантирует достижение требуемого состояния или ресурса.
- **Параметры:**
  - `currentValue` — входной параметр метода.
  - `maxValue` — входной параметр метода.
  - `entityName` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function ensureRoomCapacityForJoin(roomId, peerId)`
- **Контекст:** `(global)`
- **Назначение:** Гарантирует достижение требуемого состояния или ресурса.
- **Параметры:**
  - `roomId` — входной параметр метода.
  - `peerId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function ensurePeerJoined(roomId, peerId)`
- **Контекст:** `(global)`
- **Назначение:** Гарантирует достижение требуемого состояния или ресурса.
- **Параметры:**
  - `roomId` — входной параметр метода.
  - `peerId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function failWithCode(code, message)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `code` — входной параметр метода.
  - `message` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function ensureNotShuttingDown()`
- **Контекст:** `(global)`
- **Назначение:** Гарантирует достижение требуемого состояния или ресурса.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function handleRequest(request)`
- **Контекст:** `(global)`
- **Назначение:** Обрабатывает событие/команду и запускает соответствующий сценарий.
- **Параметры:**
  - `request` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opGetCapabilities(id)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `function toFiniteNumber(value)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `value` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function summarizeStatsRows(rows)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `rows` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function normalizeInjectedStats(value)`
- **Контекст:** `(global)`
- **Назначение:** Нормализует формат входных данных для дальнейшей обработки.
- **Параметры:**
  - `value` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function buildInjectedStatsFromPayload(payload)`
- **Контекст:** `(global)`
- **Назначение:** Собирает производную структуру/результат из входных данных.
- **Параметры:**
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function opGetMediaStats(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opCreateRouter(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opJoinPeer(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opLeavePeer(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opClosePeer(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opCreateWebRtcTransport(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opConnectDtls(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opAddIceCandidate(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opProduce(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opPauseProducer(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opResumeProducer(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opCloseProducer(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opConsume(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function opResumeConsumer(id, payload)`
- **Контекст:** `(global)`
- **Назначение:** Выполняет прикладную операцию протокола/бизнес-команды.
- **Параметры:**
  - `id` — входной параметр метода.
  - `payload` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `function makeSuccess(id, message, data, router)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `id` — входной параметр метода.
  - `message` — входной параметр метода.
  - `data` — входной параметр метода.
  - `router` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function makeFailure(id, code, message)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `id` — входной параметр метода.
  - `code` — входной параметр метода.
  - `message` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function requireString(value, fieldName)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `value` — входной параметр метода.
  - `fieldName` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function ensurePeerSet(roomId)`
- **Контекст:** `(global)`
- **Назначение:** Гарантирует достижение требуемого состояния или ресурса.
- **Параметры:**
  - `roomId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function getTransportEntry(transportId)`
- **Контекст:** `(global)`
- **Назначение:** Получает и возвращает данные из текущего контекста/хранилища.
- **Параметры:**
  - `transportId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function createWorkerContext()`
- **Контекст:** `(global)`
- **Назначение:** Создаёт новый объект/ресурс и подготавливает его к использованию.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `function removeWorkerContext(workerContext)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `workerContext` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function getWorkerContextById(workerId)`
- **Контекст:** `(global)`
- **Назначение:** Получает и возвращает данные из текущего контекста/хранилища.
- **Параметры:**
  - `workerId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function workerDiagnostics()`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function pickWorkerContextForRoom(roomId)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `roomId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function getRoomWorkerContext(roomId)`
- **Контекст:** `(global)`
- **Назначение:** Получает и возвращает данные из текущего контекста/хранилища.
- **Параметры:**
  - `roomId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function getRoomRouter(roomId)`
- **Контекст:** `(global)`
- **Назначение:** Получает и возвращает данные из текущего контекста/хранилища.
- **Параметры:**
  - `roomId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function ensureWorkerPool()`
- **Контекст:** `(global)`
- **Назначение:** Гарантирует достижение требуемого состояния или ресурса.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function ensureCapabilityRouter()`
- **Контекст:** `(global)`
- **Назначение:** Гарантирует достижение требуемого состояния или ресурса.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `async function ensureRoomRouter(roomId)`
- **Контекст:** `(global)`
- **Назначение:** Гарантирует достижение требуемого состояния или ресурса.
- **Параметры:**
  - `roomId` — входной параметр метода.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

#### `function safeCloseTransportById(transportId)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `transportId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function safeCloseProducerById(producerId)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `producerId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function safeCloseConsumerById(consumerId)`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `consumerId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function closePeerResources(roomId, peerId)`
- **Контекст:** `(global)`
- **Назначение:** Закрывает ресурс и выполняет очистку зависимых сущностей.
- **Параметры:**
  - `roomId` — входной параметр метода.
  - `peerId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function closeRoom(roomId)`
- **Контекст:** `(global)`
- **Назначение:** Закрывает ресурс и выполняет очистку зависимых сущностей.
- **Параметры:**
  - `roomId` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function normalizeWsPath(pathValue)`
- **Контекст:** `(global)`
- **Назначение:** Нормализует формат входных данных для дальнейшей обработки.
- **Параметры:**
  - `pathValue` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `function log(message)`
- **Контекст:** `(global)`
- **Назначение:** Формирует и отправляет диагностическую запись.
- **Параметры:**
  - `message` — входной параметр метода.
- **Возвращает:** Возвращает значение через `return` или завершает выполнение без явного результата.

#### `async function shutdown()`
- **Контекст:** `(global)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - Нет входных параметров.
- **Возвращает:** Возвращает `Promise` (результат через resolve/reject).

---

### `scripts/chat-heavy-load.py`
- **Пространство/модуль:** `(python-module)`
- **Абстракции (типы):** Counters, StageDurations, Config, ClientState
- **Количество методов/функций:** 2

#### `async def run_limited(items: List[Any], limit: int, worker)`
- **Контекст:** `(python-module)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `items: List[Any]` — входной параметр метода.
  - `limit: int` — входной параметр метода.
  - `worker` — входной параметр метода.
- **Возвращает:** Асинхронная coroutine; результат передаётся через `return` внутри async-функции.

#### `async def wrapped(i: int, item: Any)`
- **Контекст:** `(python-module)`
- **Назначение:** Инкапсулирует локальную часть бизнес-логики/инфраструктурной логики в рамках текущего модуля.
- **Параметры:**
  - `i: int` — входной параметр метода.
  - `item: Any` — входной параметр метода.
- **Возвращает:** Асинхронная coroutine; результат передаётся через `return` внутри async-функции.

---

### `scripts/media-plane-10x10x10.py`
- **Пространство/модуль:** `(python-module)`
- **Абстракции (типы):** Config, Metrics, WsClient
- **Количество методов/функций:** 0

_В этом файле не обнаружены объявленные методы/функции (возможны только DTO/константы/markup/данные)._
